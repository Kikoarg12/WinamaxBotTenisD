services:
  - type: worker
    name: winamax-tenis-bot
    env: python
    buildCommand: |
      pip install -r requirements.txt
      playwright install chromium
    startCommand: python main.py
    region: frankfurt